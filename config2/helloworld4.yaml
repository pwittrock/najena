apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: helloworld4
  name: helloworld4
spec:
  replicas: 3
  minReadySeconds: 10
  selector:
    matchLabels:
      app: helloworld4
  template:
    metadata:
      labels:
        app: helloworld4
    spec:
      containers:
      - image: pwittrock/gs-spring-boot-docker
        name: najena
        resources:
          limits:
            memory: "512Mi"
            cpu: "100m"
          requests:
            memory: "512Mi"
            cpu: "100m"
        env:
        - name: MESSAGE
          value: "Hello from app 4 - 5"
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: helloworld4
  name: helloworld4
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: helloworld4
  type: LoadBalancer
