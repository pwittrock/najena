apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: helloworld1
  name: helloworld1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: helloworld1
  template:
    metadata:
      labels:
        app: helloworld1
    spec:
      containers:
      - image: pwittrock/gs-spring-boot-docker
        name: najena
        env:
        - name: MESSAGE
          value: "Hello from app 1 - 3"
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: helloworld1
  name: helloworld1
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: helloworld1
  type: LoadBalancer
